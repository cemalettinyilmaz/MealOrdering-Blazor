@inherits ButtonComponent

<ButtonComponent Caption="@(Caption ?? "Edit")"
                 Color="@(Color ?? "secondary")"
                 Icon="@(Icon ?? "pencil")"
                 OnClick="OnClick"
                 ToolTipStr="@(ToolTipStr ?? "Edit")"
                 IsOutline="@IsOutline"
                 IsSmall="@(IsSmall)"
                 IsBusyCaption="Dur Hele"></ButtonComponent>
